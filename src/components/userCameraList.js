import React, { Component } from "react";
import { View, Text, ScrollView, StyleSheet } from "react-native";

export default class userCameraList extends Component {
  static navigationOptions = {
    title: "选择上传图片"
  };
  render() {
    return <Text>相册</Text>;
  }
}
