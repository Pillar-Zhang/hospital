spring:
  datasource:
    url: jdbc:mysql://localhost:3306/hospital?charset=utf8mb4&useSSL=false
    username: root
    password: Lww..123
    driver-class-name: com.mysql.jdbc.Driver
  jpa:
    show-sql: true
    database-platform: org.hibernate.dialect.MySQL5InnoDBDialect
    hibernate:
      ddl-auto: update
server:
  port: 8080

swagger:
  base-package: net.vivipara
  base-path: /**
